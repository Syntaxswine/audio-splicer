{
  "name": "audio-splicer",
  "version": "1.5.0",
  "description": "Merge audio files: sequential concat or fixed-length random splice with novelty across runs",
  "type": "module",
  "bin": {
    "splice": "bin/splice.mjs"
  },
  "scripts": {
    "test": "node tools/run-tests.mjs && node tools/test-app.mjs"
  },
  "license": "ISC",
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0"
  }
}
